<p style="color: green"><%= notice %></p>

<h1>Categories</h1>
<div class="container">
<div class="row">
<%= link_to "New category", new_category_path %>
  <% @categories.each do |category| %>
  <div class="col-md-4">
        <div class="card">
          <div class="card-body">
    <%= render category %>
    </div>        
        </div>
      </div>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>
</div>


<i class="fa fa-arrow-left" aria-hidden="true"></i><%= link_to " Back", '/' %>